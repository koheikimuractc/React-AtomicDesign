import styled from "styled-components";

export const Top = () => {
  return (
    <SContainer>
      <h2>TOP PAGE NOW</h2>
    </SContainer>
  );
};

const SContainer = styled.div`
  text-align: center;
`;
